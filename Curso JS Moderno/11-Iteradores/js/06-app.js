const pendientes = ['Tarea', 'Comer', 'Proyecto', 'Ejercitarse', 'Estudiar']

pendientes.forEach((p, i )=> console.log(`${i}:${p}`));
const arreglo = pendientes.map((p, i )=> (p));

console.log(arreglo)